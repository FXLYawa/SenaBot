import {beforeEach,describe,expect,it} from "vitest";import {defaultSettings,loadSettings,saveSettings} from "./settings";describe("settings",()=>{beforeEach(()=>localStorage.clear());it("loads defaults",()=>expect(loadSettings()).toEqual(defaultSettings));it("persists and merges settings",()=>{saveSettings({...defaultSettings,characterName:"星奈"});expect(loadSettings().characterName).toBe("星奈")});it("recovers from invalid storage",()=>{localStorage.setItem("senabot.desktop.settings","{");expect(loadSettings()).toEqual(defaultSettings)})});
