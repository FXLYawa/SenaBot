import {describe,expect,it} from "vitest";import {isServerMessage} from "./protocol";describe("isServerMessage",()=>{it("accepts the adapter frame",()=>expect(isServerMessage({type:"message",text:"你好",reply_to:null})).toBe(true));it.each([null,{type:"message",text:2,reply_to:null},{type:"other",text:"x",reply_to:null},{type:"message",text:"x"}])("rejects malformed frames",value=>expect(isServerMessage(value)).toBe(false))});
