import {render,screen} from "@testing-library/react";import userEvent from "@testing-library/user-event";import {describe,expect,it,vi} from "vitest";import {MessageComposer} from "./MessageComposer";describe("MessageComposer",()=>{it("sends with Enter but not Shift+Enter",async()=>{const send=vi.fn();const setDraft=vi.fn();const user=userEvent.setup();render(<MessageComposer draft="你好" setDraft={setDraft} onSend={send} enabled sendWithEnter/>);await user.type(screen.getByLabelText("输入消息"),"{enter}");expect(send).toHaveBeenCalledOnce();send.mockClear();await user.type(screen.getByLabelText("输入消息"),"{shift>}{enter}{/shift}");expect(send).not.toHaveBeenCalled()});it("disables sending when disconnected",()=>{render(<MessageComposer draft="你好" setDraft={()=>{}} onSend={()=>{}} enabled={false} sendWithEnter/>);expect(screen.getByLabelText("发送消息")).toBeDisabled()})});
