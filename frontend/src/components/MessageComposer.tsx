export function MessageComposer({draft,setDraft,onSend,enabled,sendWithEnter}:{draft:string;setDraft(v:string):void;onSend():void;enabled:boolean;sendWithEnter:boolean}){return <div className="composer"><textarea aria-label="输入消息" value={draft} disabled={!enabled} placeholder={enabled?"和她说点什么……":"等待本地服务连接……"} rows={1} onChange={e=>setDraft(e.target.value)} onKeyDown={e=>{if(sendWithEnter&&e.key==="Enter"&&!e.shiftKey){e.preventDefault();onSend();}}}/><button aria-label="发送消息" onClick={onSend} disabled={!enabled||!draft.trim()}><span aria-hidden="true">→</span></button></div>}
